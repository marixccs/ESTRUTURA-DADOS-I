/*
    1. Utilizando o arquivo "PILHA" da pasta "lib".
    Descreva todo o código abaixo a partir do estado da pilha (inicialmente vazia)
    e após cada uma das operações a seguir:
                    // Estado inicial: []
    push(5)         // Insere 5 no topo: [5]
    push(9)         // Insere 9 no topo: [5, 9]
    pop()           // Remove 9 do topo: [5]
    push(3)         // Insere 3 no topo: [5, 3]
    push(8)         // Insere 8 no topo: [5, 3, 8]
    pop()           // Remove 8 do topo: [5, 3]
    pop()           // Remove 3 do topo: [5]
    push(6)         // Insere 6 no topo: [5, 6]
    push()          // Insere undefined (pois não foi passado valor): [5, 6, undefined]
    push(2)         // Insere 2 no topo: [5, 6, undefined, 2]
    pop()           // Remove 2 do topo: [5, 6, undefined]
    push(10)        // Insere 10 no topo: [5, 6, undefined, 10]
    push(1)         // Insere 1 no topo: [5, 6, undefined, 10, 1]
    pop()           // Remove 1 do topo: [5, 6, undefined, 10]
    pop()           // Remove 10 do topo: [5, 6, undefined]
    push(15)        // Insere 15 no topo: [5, 6, undefined, 15]
    pop()           // Remove 15 do topo: [5, 6, undefined]
    pop()           // Remove undefined do topo: [5, 6]
*/
